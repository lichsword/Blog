-------------
日期：
天气：
星期：
-------------
正文
-------------
1.完成数据库字段扩展为13个，定义了字段常量，但没有实现字面意，比如：status仍显示为1而不是"已解决"。
2.另外表格的宽度没有实现限制，这个要改css，实现内容截断。
3.要扩展对话框界面，实现13个字段的新增操作，删除操作我没测试，但估计无问题，因为html的标签属性显示data-id是正确的。
